/**
 * Authentication and user-related hooks
 * Hooks for managing authentication state and user profiles
 */

export { useAuth } from './useAuth';
export { useProfile, type UseProfileReturn } from './useProfile';
