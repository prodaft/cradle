/**
 * Thin alias for ``useNdjsonQuery`` — same NDJSON stream GET + collect behavior.
 */

export {
    useNdjsonQuery as useNdjsonStreamQuery,
    type UseNdjsonQueryOptions as UseNdjsonStreamQueryOptions,
} from './use-ndjson-query';
