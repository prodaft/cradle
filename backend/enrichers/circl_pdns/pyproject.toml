[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cradle-enricher-circl-pdns"
version = "0.0.0"
description = "CRADLE Docker enricher: CIRCL PDNS"
requires-python = ">=3.14"
dependencies = [
    "pypdns>=2.0,<3",
]

[tool.setuptools]
packages = []
