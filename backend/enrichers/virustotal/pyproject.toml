[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cradle-enricher-virustotal"
version = "0.0.0"
description = "CRADLE Docker enricher: VirusTotal"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32,<3",
]

[tool.setuptools]
packages = []
