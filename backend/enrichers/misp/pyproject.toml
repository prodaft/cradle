[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cradle-enricher-misp"
version = "0.0.0"
description = "CRADLE Docker enricher: MISP"
requires-python = ">=3.14"
dependencies = [
    "pymisp>=2.4,<3",
]

[tool.setuptools]
packages = []
