"""Statistics app providing homepage dashboard data (recent notes, entities, artifacts)."""

from django.apps import AppConfig


class StatisticsConfig(AppConfig):
    """Django app config for statistics."""

    name = "statistics"
