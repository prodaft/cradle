"""Django app configuration for notifications."""

from django.apps import AppConfig


class NotificationsConfig(AppConfig):
    """App config for user notifications (access, reports, enrichments)."""

    name = "notifications"
